{
    // ========================================
    // Autodesk Dynamo MCP 配置檔案模板
    // ========================================
    // 📋 說明：此為配置模板，包含詳細註解。
    // 📝 使用方式：修改此檔案後，執行 deploy.ps1 會自動生成 mcp_config.json
    // ⚠️  警告：請勿直接修改 mcp_config.json，該檔案會被自動覆寫
    // ========================================
    // 👤 使用者資訊 (User Information)
    // ========================================
    // 用於識別當前使用者與權限角色
    "user_info": {
        "name": "LU", // 🔧 修改點：您的使用者名稱
        "role": "BIM Specialist" // 🔧 修改點：您的職位角色
    },
    // ========================================
    // 📂 路徑配置 (Path Configuration)
    // ========================================
    // 定義專案內各種資源的相對路徑
    "paths": {
        "scripts": "DynamoScripts", // Dynamo JSON 腳本庫目錄
        "examples": "examples", // 範例腳本目錄
        "tests": "tests", // 測試腳本目錄
        "common_nodes": "DynamoViewExtension/common_nodes.json", // 節點簽名定義檔案
        "deployment": "MCP_Listener_Package", // 套件部署資料夾
        "view_extension_src": "DynamoViewExtension" // C# 原始碼目錄
    },
    // ========================================
    // 📜 規則配置 (Rules Configuration)
    // ========================================
    // 定義專案命名規範與行為規則
    "rules": {
        "test_scripts_suffix": "_test.py", // 測試腳本的檔名後綴
        "example_scripts_prefix": "demo_", // 範例腳本的檔名前綴
        "keep_temp_files": false, // 🔧 修改點：是否保留臨時檔案 (true/false)
        "log_level": "INFO" // 🔧 修改點：記錄等級 (DEBUG/INFO/WARNING/ERROR)
    },
    // ========================================
    // 🔌 連線配置 (Connection Configuration)
    // ========================================
    // MCP 伺服器與 Dynamo 之間的通訊參數
    "connection": {
        "timeout_seconds": 5, // 🔧 修改點：連線逾時時間（秒）
        "retry_attempts": 3, // 🔧 修改點：重試次數
        "health_check_enabled": true // 是否啟用健康檢查
    },
    // ========================================
    // 🌐 伺服器配置 (Server Configuration)
    // ========================================
    // MCP 伺服器的網路參數
    "server": {
        "host": "127.0.0.1", // 🔧 修改點：伺服器主機位址（預設本機）
        "port": 65296, // 🔧 修改點：MCP Bridge 埠號（Node.js MCP Server 連線）
        "websocket_port": 65535, // 🔧 修改點：Dynamo WebSocket 監聽埠號（C# Extension 連線）
        "url_path": "/mcp/" // API 端點路徑
    },
    // ========================================
    // 🚀 部署資訊 (Deployment Information)
    // ========================================
    // 版本控制與部署步驟說明
    "deployment_info": {
        "version": "2.4", // 當前版本號
        "last_updated": "2026-01-05", // 最後更新日期
        // 📋 必要部署步驟
        "required_steps": [
            "1. Build DynamoViewExtension project (建置 C# 專案)",
            "2. Copy bin output to Dynamo packages folder (複製至套件資料夾)",
            "3. Start Dynamo and ensure ViewExtension is loaded (啟動 Dynamo 並確認插件載入)",
            "4. Run server.py (啟動 MCP 伺服器)"
        ],
        // ✨ 新功能清單
        "new_features": [
            "Log rotation (10MB limit, 3 backups) - 記錄檔自動輪替",
            "Health check endpoint (/health) - 健康檢查端點",
            "Enhanced error tracking - 強化錯誤追蹤",
            "Configurable connection timeout - 可配置連線逾時"
        ],
        "target_platform": "Windows / Revit + Dynamo" // 目標平台
    },
    // ========================================
    // 🛠️ 自動部署配置 (Auto-Deployment Settings)
    // ========================================
    // 控制自動部署行為（由 deploy.ps1 使用）
    "auto_deployment": {
        "enabled": true, // 🔧 修改點：是否啟用自動部署
        "target_dynamo_versions": [
            "3.3",
            "3.2"
        ], // 🔧 修改點：目標 Dynamo 版本列表（按優先順序）
        "backup_before_deploy": true, // 🔧 修改點：部署前是否備份舊版本
        "auto_start_server": false, // 🔧 修改點：部署後是否自動啟動 server.py
        "verify_after_deploy": true // 部署後是否執行驗證
    }
}